import React from 'react';
import GlobalConfigComponent from './GlobalConfig';

const GlobalConfig = () => {
  const props = {};

  return <GlobalConfigComponent {...props} />;
};

export default GlobalConfig;
