export const settingsTexts: any = {
  padding_mm:
    'Distance from the position of the limit switch press registration to the edge of the range in which stepper will move. Both front and back.',
  stroke_limit: 'Safety limit to prevent too deep strokes.',
  max_stroke:
    'Max stroke between limit switches activation, max working range is smaller by "padding_mm" * 2',
  max_steps: 'Stroke steps number, with padding steps already substracted',
};
